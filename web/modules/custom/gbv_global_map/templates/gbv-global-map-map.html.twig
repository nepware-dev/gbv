{% set url = url('<current>')['#markup']|split('/')|last %}
<div id='gbv-global-map'>
  {% if (url is defined) and (url == 'global-gbv')%}
  <div class="info">
    Click the countries in the map for details
  </div>
  {% endif %}
</div>
<div id='gbv-regional-map'></div>
<div id='gbv-country-map'></div>
